#include <algorithm>
#include <iostream>
#include <string>
#include <unordered_map>
#include <vector>
#include <cmath>
using namespace std;

int main(void) {
    int dim;
    cin >> dim;

    vector<int> points(dim);

    return 0;
}